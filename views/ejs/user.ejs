<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="style/style.css">
    <link rel="icon" href="./assets/icon.png">
    <title>Login</title>
</head>
<body>
    <div class="container-bg">
        <div class="container">
            <div class="row d-flex justify-content-center align-items-center">
                <div class="col-xl-10 mt-5">
                    <div class="card rounded border-3 shadow mt-5 p-3" id="panel">
                        <div class="text-center rounded border border-3 mb-3 p-3 border-danger">
                            <h4>Tu perfil</h4>
                        </div>
                        <div class="d-flex flex-row">
                            <div class="p-2">
                                <img src="<%= user.profileImage %>" alt="profile avatar picture" class="shadow border-bottom border-3 mb-3 border-danger" width="200">
                            </div>
                            <div class="p-2">
                                <h5>Usuario: <%= user.name %></h5>
                                <h5>Correo: <%= user.email %></h5>
                            </div>
                        </div>
                        <div class="d-flex align-items-center justify-content-center mt-4">
                            <form action="/logout?_method=DELETE" method="POST">
                                <button type="submit" class="btn efecto">Cerrar sesión</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>